version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20
        - npm install -g next
        - npm install react-icons
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
